program PhelloConsole;

{$APPTYPE CONSOLE}

uses
  SysUtils;

type
  TdoubleArray = array[1..10] of Double;

var
  //  MyTest: Char;

begin
  { TODO -oUser -cConsole Main : Insert code here }
  Writeln('hello, Delphi world!');
  Readln; // 等待键盘输入，此处的作用是让程序暂停运行，以便观察效果。

  {  MyTest := 'a';
    Writeln(High(MyTest));
    Readln;
    }
end.

 