program Project1;

{$APPTYPE CONSOLE} //进入控制台方式，即非窗体方式
var
  s, n: Integer;
begin
  // 求 0~n 间的所有偶数

  write('n=?');
  Readln(n);
  //    s := 1;
  //    while s < n do
  //    begin
  //      if s mod 2 = 0 then
  //      begin
  //        Writeln(s);
  //      end;
  //      s := s + 1;
  //    end;

  s := n - 1;
  while s > 1 do
  begin
    if not (s mod 2 = 0) then
    begin
      Writeln(s);
    end;
    s := s - 1;
  end;

  Readln; //等待按任意键
end.

